language: go

go:
- 1.16.x
- master

services:
  - postgresql

jobs:
  include:
    - name: unit tests
      script: go test -v ./... -tags unit
    - name: integration tests
      script: go test -v ./... -tags integration