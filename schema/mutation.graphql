# Mutation schemas in the API
type Mutation {
    # Add a team to watchlist
    addTeam(name: String!, abbr: String!, league: String!): Team
    # Add a player to a watchlist
    addPlayer(firstName: String!, lastName: String!, teamName: String!): Player 
}
